---
name: code-review
description: "对代码进行结构化审查，支持三种模式：Git 变更审查（本次修改）、项目全量审查、文件定向审查。自动识别前后端技术栈并生成审查配置模板。当用户要求审查代码、检查代码质量、提交前审查、或审查 git 变更时调用。"
---

# Code Review

对代码进行结构化审查。自动识别前后端技术栈，首次使用时生成审查配置模板，后续调用基于模板进行审查。

## Intent

当用户要求审查、检查、分析代码时触发此技能。支持以下文件类型：

**前端**：
- `.tsx`, `.ts`, `.jsx`, `.js`, `.vue`, `.svelte`
- `.css`, `.scss`, `.less`
- `.html`

**后端**：
- `.py`, `.java`, `.go`, `.ts`, `.js`, `.rb`, `.php`, `.rs`, `.kt`, `.scala`
- `.sql`, `.graphql`
- `Dockerfile`, `docker-compose.yml`
- `.yaml`, `.yml`, `.toml`, `.json`（配置文件）

## Review Modes

根据用户意图自动判断使用哪种模式。如果用户没有明确指定，**默认使用模式 B（Git 变更审查）**。

### Mode A: 项目全量审查

**触发条件**：用户明确说"审查整个项目"、"全量审查"、"检查所有代码"
**审查范围**：项目中所有代码文件（排除配置中的排除路径）
**适用场景**：项目初始化后的首次审查、定期代码健康检查

### Mode B: Git 变更审查（默认）

**触发条件**：用户说"审查我的修改"、"检查这次改动"、"提交前审查"、"review 一下"、或任何未明确指定范围的审查请求
**审查范围**：仅本次 git 变更涉及的文件和代码行
**适用场景**：日常开发中的小改动、提交前的快速检查

支持以下 git 范围（按优先级自动判断）：

| 优先级 | 范围 | Git 命令 | 说明 |
|--------|------|----------|------|
| 1 | Staged 变更 | `git diff --cached` | 已 `git add` 但未 commit 的变更 |
| 2 | Unstaged 变更 | `git diff` | 已修改但未 `git add` 的变更 |
| 3 | 最近一次 commit | `git diff HEAD~1 HEAD` | 最后一次提交的变更 |
| 4 | 当前分支 vs 主分支 | `git diff main...HEAD` 或 `git diff master...HEAD` | 当前分支所有变更 |

如果同时存在 staged 和 unstaged 变更，**优先审查 staged 变更**，并提示用户还有 unstaged 变更未审查。

### Mode C: 文件定向审查

**触发条件**：用户指定了具体文件或目录路径
**审查范围**：用户指定的文件或目录
**适用场景**：审查特定组件、模块、API

## First-Time Setup: 技术栈检测与模板初始化

### 检测流程

首次调用时，检查项目根目录下是否存在配置文件 `.skills/code-review/.review-config.md`。

**如果不存在**，执行以下初始化流程：

1. **扫描项目根目录**，检测以下信息：

| 检测项 | 检测方式 |
|--------|----------|
| 项目类型 | 前端/后端/全栈（根据文件类型判断） |
| 语言 | 检测 `.py`/`.java`/`.go`/`.ts`/`.js`/`.vue`/`.tsx` 等文件占比 |
| 前端框架 | React/Vue/Angular/Svelte/Next.js/Nuxt 等 |
| 后端框架 | Django/Flask/FastAPI/Spring Boot/Express/Gin/Rails 等 |
| 数据库 | MySQL/PostgreSQL/MongoDB/Redis 等 |
| ORM | SQLAlchemy/Prisma/TypeORM/GORM/Django ORM 等 |
| CSS 方案 | Tailwind CSS / CSS Modules / Styled-components / Sass 等 |
| 测试框架 | Jest/Vitest/pytest/JUnit/Go testing 等 |
| API 类型 | REST/GraphQL/gRPC |
| 代码规范 | ESLint/Prettier/Pylint/Black/Go fmt 等 |
| 包管理器 | npm/yarn/pnpm/pip/poetry/maven/go modules 等 |

2. **生成配置文件** `.skills/code-review/.review-config.md`，内容如下：

```markdown
# 代码审查配置

> 此文件由 code-review skill 自动生成，可根据项目需要手动修改。

## 项目技术栈

- **项目类型**: [前端/后端/全栈]
- **语言**: [检测到的语言及版本]
- **前端框架**: [检测到的前端框架]
- **后端框架**: [检测到的后端框架]
- **数据库**: [检测到的数据库]
- **ORM**: [检测到的 ORM]
- **CSS 方案**: [检测到的 CSS 方案]
- **测试框架**: [检测到的测试框架]
- **API 类型**: [REST/GraphQL/gRPC]
- **代码规范**: [检测到的规范工具]
- **包管理器**: [检测到的包管理器]

## 审查规则配置

### 启用的检查类别（取消勾选以禁用）

#### 通用规则（前后端都适用）
- [x] 代码质量（命名、类型安全、代码结构）
- [x] 安全性（注入攻击、认证授权、敏感数据）
- [x] 错误处理（异常捕获、日志记录、降级策略）

#### 前端专属规则
- [x] 性能（重渲染、内存泄漏、代码分割）
- [x] 可访问性（语义化 HTML、ARIA、键盘导航）
- [x] 样式（CSS 规范、响应式、设计 token）

#### 后端专属规则
- [x] 性能（数据库查询、缓存、并发处理）
- [x] API 设计（RESTful 规范、错误响应、版本控制）
- [x] 数据库（索引、事务、数据一致性）

### 项目特定规则（可自定义添加）

<!-- 在此添加项目特有的审查规则 -->
<!-- 例如：
- 所有 API 必须使用统一的响应格式
- 组件 Props 必须定义 TypeScript interface
- 数据库操作必须使用事务
-->

### 排除路径（不审查的目录/文件）

- `node_modules/`
- `venv/`
- `__pycache__/`
- `dist/`
- `build/`
- `coverage/`
- `*.min.js`
- `*.test.*` / `*.spec.*`（除非明确要求审查测试）
- `migrations/` / `migrate/`（数据库迁移文件）

## 审查严重级别定义

- **P0（紧急）**: 安全漏洞、数据丢失、生产崩溃、认证绕过
- **P1（重要）**: API 契约错误、用户可见回归、数据库死锁、内存泄漏
- **P2（建议）**: 可维护性问题、性能隐患、缺少测试
- **P3（优化）**: 代码风格、命名优化、注释补充
```

3. **提示用户**：告知已检测到技术栈并生成了配置文件，用户可以手动编辑该文件来自定义审查规则。

### 后续使用

如果配置文件已存在，直接读取配置并基于其中的规则进行审查。

---

## Review Process

### Mode A: 项目全量审查

1. 扫描项目中所有代码文件（排除配置中的排除路径）
2. 按文件逐一检查
3. 生成全量报告

### Mode B: Git 变更审查（重点）

#### Step 1: 获取变更范围

```bash
# 1. 先检查是否有 staged 变更
git diff --cached --stat
git diff --cached

# 2. 如果没有 staged，检查 unstaged 变更
git diff --stat
git diff

# 3. 如果都没有，检查最近一次 commit
git diff HEAD~1 HEAD --stat
git diff HEAD~1 HEAD

# 4. 如果都没有，检查当前分支 vs 主分支
git diff main...HEAD --stat  # 或 master...HEAD
git diff main...HEAD
```

#### Step 2: 过滤代码文件

从变更文件列表中，只保留代码相关文件（前端和后端文件类型），排除配置中的排除路径。

如果没有代码文件变更，提示用户"本次变更不涉及代码文件"。

#### Step 3: 生成业务级变更概述

在分析 diff 的基础上，用**自然语言**总结本次变更的业务含义。这是让开发者一眼看懂"这次改了什么"的关键。

**生成方法**：
1. 读取所有变更文件的 diff 内容
2. 理解每个文件变更的业务意图（新增/修改/删除了什么功能）
3. 用简洁的中文概述每条业务变更

**概述要求**：
- 用**业务语言**而非技术语言描述
- 每条概述控制在一句话内
- 按影响范围从大到小排列
- 标注变更类型：新增 / 修改 / 移除 / 重构 / 修复

**示例**：
```
- [新增] 用户注册接口，支持邮箱验证码校验
- [修改] 用户列表页移除"删除用户"按钮及相关确认弹窗
- [修复] 修复并发场景下优惠券重复使用的问题
- [重构] 将支付回调处理从 Controller 迁移到 Service 层
```

#### Step 4: 分析变更摘要

生成技术层面的变更摘要，包括：
- 变更文件数量
- 新增/修改/删除的文件列表
- 变更行数统计（+N / -N）

#### Step 5: 逐文件审查变更

对每个变更文件：

1. **读取 diff 内容**：只关注变更的代码行（`+` 和 `-` 行）
2. **读取上下文**：读取变更行周围的相关代码（前后各 20 行），理解模块结构、依赖关系
3. **检查变更部分**：根据配置中启用的检查类别，只审查变更的代码，不审查未变更的代码
4. **检查变更引入的问题**：
   - 新增代码是否引入了 bug
   - 修改代码是否破坏了原有逻辑
   - 删除代码是否影响了其他依赖
   - 变更是否与项目规范一致

#### Step 6: 生成变更级报告

### Mode C: 文件定向审查

1. 读取用户指定的文件
2. 按检查类别全量审查
3. 生成文件级报告

---

## Checklist

详细审查规则见 [references/checklist.md](references/checklist.md)。

按配置文件中启用的检查类别，逐一检查：

### 通用规则（前后端都适用）

- **代码质量**：命名、类型安全、职责单一、重复代码、导入导出
- **安全性**：注入攻击（SQL/XSS）、认证授权、敏感数据保护、CSRF
- **错误处理**：异常捕获、日志记录、降级策略、重试机制

### 前端专属规则

- **性能**：重渲染、虚拟滚动、懒加载、内存泄漏、代码分割
- **可访问性**：语义化 HTML、ARIA、键盘导航、颜色对比度
- **样式**：CSS 规范、响应式、未使用样式、设计 token 一致性

### 后端专属规则

- **性能**：数据库查询优化、缓存使用、并发处理、内存管理
- **API 设计**：RESTful 规范、错误响应、版本控制、幂等性
- **数据库**：索引使用、事务管理、数据一致性、N+1 查询

---

## Output Format

### Mode B Output: Git 变更审查报告（默认）

#### Template B-A（发现问题时）

```markdown
# 代码审查报告（Git 变更）

**审查模式**: Git 变更审查
**变更范围**: [staged / unstaged / 最近 commit / 分支对比]
**变更文件**: N 个代码文件（+X 行 / -Y 行）
**审查时间**: [当前时间]
**技术栈**: [从配置文件读取]

---

## 本次改了什么

> 用自然语言概述本次变更的业务含义，让 reviewer 快速理解变更意图。

- [新增] 用户注册接口，支持邮箱验证码校验
- [修改] 用户列表页移除"删除用户"按钮
- [修复] 修复并发场景下优惠券重复使用问题

---

## 技术变更摘要

| 文件 | 状态 | 变更行数 |
|------|------|----------|
| `src/api/user.py` | 修改 | +45 / -3 |
| `src/components/UserList.tsx` | 修改 | +15 / -28 |
| `src/services/coupon.py` | 修改 | +8 / -2 |

---

## 紧急问题 (P0) - 共 N 个

### 1. [问题标题]
- **文件**: `path/to/file.py:行号`（变更行）
- **类别**: [代码质量/安全性/性能/API设计/错误处理/数据库/可访问性/样式]
- **问题描述**: [具体描述问题是什么]
- **影响**: [说明这个问题的影响]
- **变更内容**:
  ```diff
  - // 原来的代码
  + // 变更后的代码（有问题的）
  ```
- **建议修复**:
  ```代码
  // 建议的修复代码
  ```

---

## 重要问题 (P1) - 共 N 个
（同上格式）

## 建议改进 (P2) - 共 N 个
（同上格式）

## 优化建议 (P3) - 共 N 个
（同上格式）

---

## 审查摘要

| 类别 | 紧急(P0) | 重要(P1) | 建议(P2) | 优化(P3) |
|------|----------|----------|----------|----------|
| 代码质量 | 0 | 1 | 2 | 1 |
| 安全性 | 1 | 0 | 0 | 0 |
| 性能 | 0 | 1 | 1 | 0 |
| API 设计 | 0 | 0 | 1 | 0 |
| 错误处理 | 0 | 0 | 0 | 1 |
| 数据库 | 0 | 1 | 0 | 0 |
| **合计** | **1** | **3** | **4** | **2** |

---

是否需要我帮你修复这些问题？
```

#### Template B-B（无问题时）

```markdown
# 代码审查报告（Git 变更）

**审查模式**: Git 变更审查
**变更范围**: [staged / unstaged / 最近 commit / 分支对比]
**变更文件**: N 个代码文件（+X 行 / -Y 行）
**审查时间**: [当前时间]

## 本次改了什么

- [修改] 用户查询接口增加分页参数校验

## 技术变更摘要

| 文件 | 状态 | 变更行数 |
|------|------|----------|
| `src/api/user.py` | 修改 | +5 / -2 |

## 审查结果

未发现需要修复的问题。本次变更代码质量良好。
```

### Mode A/C Output: 全量/文件审查报告

使用与 Mode B 相同的输出结构，但"本次改了什么"替换为"审查范围概述"，"技术变更摘要"替换为完整的文件列表。

---

## Rules

1. **Git 变更审查只审查变更的代码**，不审查未变更的部分，保持报告简洁
2. 但需要**读取上下文代码**来理解变更的影响范围
3. 审查结果应具体、可操作，不要给出模糊的建议
4. 每个问题都要指出具体的文件和行号，并展示相关的 diff 内容
5. 如果问题数量超过 10 个，先输出"10+ 个问题"，然后只展示前 10 个最严重的问题
6. 不要包含主观偏好，只关注客观的代码质量
7. 审查完成后，询问用户是否需要帮助修复问题
8. 如果用户没有明确指定审查模式，**默认使用 Git 变更审查**
9. 如果没有发现问题，使用 Template B-B 输出简洁的"无问题"报告
10. 对于数据库相关变更，特别注意索引、事务、数据迁移的影响
11. 对于 API 变更，检查向后兼容性和版本控制
